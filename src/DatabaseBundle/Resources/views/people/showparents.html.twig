{% extends 'DatabaseBundle:base:base.html.twig' %}

{% block title %}
  Mostrando a todos los padres
{% endblock %}

{% block body %}
<h1>Padres de la escuela</h1>
{% if seasonNumber == 0 %}
    <span> No se han añadido temporadas todavía. <a href="/season/newSeason">Añade una</a></span>
{% else %}
  {{ form_start(seasonForm) }}
  <table>
    <td> {{ form_row(seasonForm.season) }} </td>
    <td> {{ form_row(seasonForm.save) }} </td>
  </table>
  {{ form_end(seasonForm) }}
  <table style="width: 100%">
  <tr>
    <th>Padre</th>
    <th>Hijo</th>
    <th>Categoría</th>
    <th>Número de teléfono</th>
    <th>Email</th>
  </tr>
  {% for data in parentData %}
    {% for child in data.getPlayerData() %}
      <tr>
        <td> {{ data.getPersonalData().getName() }}
             {{ data.getPersonalData().getSurname() }} </td>
        <td> {{ child.getPersonalData().getName() }}
             {{ child.getPersonalData().getSurname() }} </td>
        <td> {{ child.getCategory()}} </td>
        <td> {{ data.getPersonalData().getPhone() }} </td>
        <td> {{ data.getPersonalData().getEmail() }} </td>
        <td> <a href="/person/editPerson/{{data.getId()}}/{{season.getId()}}"> Editar </a> </td>
        <td> <a href="/person/deletePerson/{{data.getId()}}" onclick="return confirm('¿Estás seguro de querer borrar este miembro?')"> Eliminar </a> </td>
      </tr>
    {% endfor %}
  {% endfor %}
  </table>
{% endif %}
{% endblock %}
